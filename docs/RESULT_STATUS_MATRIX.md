# Result status matrix

| Item | Status | Evidence |
|---|---|---|
| Static separated-code holonomy | Falsified | symmetry no-go, filament scaling, inertia theorem |
| Corrected same-edge cat control | Validated | Phase 2 and Phase 3 scripts |
| Scalar Gaussian exchange signature | Validated | Phase 4 convergence and null control |
| Full logical Gaussian phase gate | Validated | singular values, polar decomposition, theta scans |
| Gaussian topological path invariance | Falsified at finite depth | `results/phase4_2/path_robustness.csv` |
| Digital simultaneous swap | Rejected | composite degeneracy and large leakage |
| Digital sequential swap | Validated | `results/phase4_3/*_seq.json` |
| Distance invariance R=3..5 | Validated numerically | spread in slope ~3.5e-4 |
| Localization convergence toward -theta | Strong numerical evidence | power-law fit, offset compatible with zero |
| Exact ideal Fock-path phase | Proven for implemented convention | exact path enumeration |
| Exact isolated two-level handoff phase | Proven/numerically exact | max error <2.6e-14 |
| First-order odd dynamical contribution at theta=0 | Analytic identity | real H, imaginary antisymmetric derivative |
| Static disorder 1%..20% | Validated, two seeds each | disorder summary JSON/CSV |
| Static disorder >20% | Incomplete | raw logs only |
| Digital theta=0.6 and 0.9 | Validated numerically | high-fidelity diagonal phase family |
| Digital dt=0.125 convergence point | Validated numerically | leakage 3.02e-5 |
| Current braid non-commutativity | Falsified | commuting Z family, Lie rank 1 |
| Phase 4.6 fast local-Trotter kernel | Exploratory | qualitatively consistent, cross-integrator offset remains |
| D=8 deep-trap run | Incomplete | raw log only |
| Phase 4.7 deep-limit D=4..12 | Numerically closed for the Abelian primitive | all five depths are now at `dt=0.125`; fit `odd_slope=-1+aD^-p` gives `a=0.66493`, `p=2.28624±0.00764`, `R²=0.999983`, with leakage `<5e-5`, `sigma_min>0.999975` and positive handoff gaps. This is not a non-Abelian/topological-computation claim. `results/phase4_7/publication_closure.json`. |
| Phase 4.7 digital path invariance | Validated at tested operating point | 11 finite-depth deformations: max phase shift 7.65e-5 and max logical-unitary distance 5.41e-5; not a standalone topology claim |
| Phase 4.7 composition n=1,2,4,8 | Validated at the tested operating point | raw-transport branch reconstruction gives phase additivity error up to 2.57e-2 rad at n=8, leakage <=2.46e-4, no superlinear leakage signature; still an Abelian Z family |
| N=3 pinned mediator: noncommuting algebra | Falsified for tested route | `||[U_A,U_B]||=5.80e-8 < 1e-5`; braid residual explicitly non-interpretable |
| N=3 pinned mediator: adiabatic-control quality | Not qualified for tested route | internal split ~1.02e-6, isolation gap ~1.25e-3, minimum Kato-link singular value 0.664 |
| Synthetic SU(2) matrix-link transport | Validated as imposed-link implementation | prescribed Wilson links reproduce through digital transport, including JW-string N=2 audit; not emergent non-Abelianity |
| Synthetic L=14 spinor code as passive topological memory | Falsified | local distinguishability norm ~1.396 despite spectral isolation |
| Minimal number-conserving two-wire pair-hopping bridge | Rejected for scanned finite systems | no L=6 candidate with edge-localized local transfer; best split/gap 7.10e-5 still has bulk/edge transfer ratio 0.705 |
| Published Iemini number-conserving parent Hamiltonian | Reproduced as an external benchmark | exact λ=1 doublet at L=4,6,8 and bulk/edge transfer ratio decreases 0.200 → 0.0769; not derived from ANTLER and no dynamic braid yet |
| Iemini finite-support braid algebra | Not qualified as a physical braid | a nonzero projected commutator is not enough: the finite-support edge generator fails the independently calibrated protection test and the neutral gap shows no saturation through L=10 |
| Native ANTLER three-leg scalar/density family | Rejected in initial scan | no `L=4,N=2` / `L=5,N=3` point meets doublet and local-indistinguishability criteria; best split/gap 0.895, local density norm 0.216 |
| Native ANTLER three-leg chiral ring-exchange family | Rejected in initial scan | best spectral split/gap 1.74e-2 but local density norm 0.331: quasi-doublet remains locally readable |
| Native flux-pair mediator interferometer | Local microscopic mechanism qualified | exact N=1 pi-flux Schur cancellation `3.48e-19`; interaction-dependent N=2 pair splitting follows `t^3.9938`, but this is not yet a tiled ANTLER code or braid |
| Charge-two mediator local block | Local exact-symmetry mechanism qualified | both branch-parity commutators and all N=1 cross transfer vanish exactly; pair splitting follows `g^1.9964`; requires a new charge-two degree of freedom |
| Charge-two mediator ladder | Rejected in registered initial scan | exact branch parities and low density distinguishability, but no L=4,5 point reaches split/gap `<1e-3`; best L=4 ratio `1.96e-3` worsens to `3.53e-2` at L=5 |
| Iemini bond interaction, charge-two mediator factorization | Validated as an external microscopic bridge | local interaction is negative semidefinite rank 3 and equals `-C†C` to `1.26e-15`; not a distinct ANTLER phase |
| Multiplet-mediator parent convergence | Validated as an external-parent convergence test | fixed three-channel construction has exact generalized parities; overlap with external frame reaches `0.99536`, and logical split extrapolates to zero in `1/Delta` |
| Kitaev projected-edge harness control | Validated diagnostic calibration | exact ideal-chain Majorana has normalized `||(1-P)[H,O]P||=0`, while a bulk Majorana gives `2.0`; this validates the test implementation only |
| Finite-support edge-operator protection test | Not qualified at tested sizes | external Iemini truncated edge generator has large `||(1-P)[H,O]P||`; at fixed `j=3`, its normalized residual is nearly flat, `6.3184` (L=8) to `6.3057` (L=10) |
| First-order SW dressing of mediator-bridge edge operator | Necessary but insufficient | removes virtual-mediator divergence, but normalized projected commutator plateaus near `9.5` at Delta=80..640; support/dressing convergence is still missing |
| External edge-operator support scaling through L=10 | Not qualified; size and support axes separated | the apparent maximal-support decrease changes `j` with `L`; at fixed `j=3` the residual is effectively flat and large, so no protection scaling is demonstrated |
| Charge-sector incompressibility of external parent | Falsified | addition/removal energies vanish numerically at L=4,6,8; a separately computed neutral gap falls `1.462 -> 0.717 -> 0.418 -> 0.272` through L=10, with no demonstrated saturation |
| Phase 7 parent-Hamiltonian audit harness | Validated as a software control | a commuting-projector classical doublet has exact edge conservation but is rejected by its local-distinguishability probe; no physical Phase 7 Hamiltonian is claimed |
| Phase 7 fixed-point OBC fixed-point parent | Reclassified as a symmetry-restricted Ising/cat benchmark | doublet and flat neutral/charge gaps are reproduced through L=8, but `X_0` and bulk `X_2` are identical local logical operators; `C_j` is commuting PSD rather than idempotent projector |
| Phase 7 Ising edge-versus-bulk calibration | Validated as a benchmark diagnostic | at h=0.1 the signed left strong-zero recurrence matches epsilon `0.2 -> 0.00157` through support 4, while an equal bulk recurrence remains near `0.2`; not a topological claim |
| Phase 7 exhaustive local-algebra gate | Validated as a negative software control | all charge-conserving one-rung probes, including rail transfer, are generated; the Ising benchmark is rejected with non-scalar norm `1.4142` |
| Phase 7B strict quasi-1D no-go theorem | Pending formal derivation and independent audit | working argument identifies bounded-width commuting parents as the relevant class, but no formal Phase 7B proof is yet archived |
| Phase 7B 2D charge-frozen toric parent | Validated as an abstract exact reference control | `3 x 3` torus: 18 rung qubits, commuting rank-16 stabilizer algebra, GSD 4, distance 3; all 1,431 Paulis of weight <3 are scalar or null on the code |
| Phase 7B native low-body ANTLER realization of the 2D parent | Not established | no controlled Schrieffer--Wolff derivation from ANTLER hoppings/mediators to four-rung star and plaquette terms |
| Phase 7C microscopic optimizer environment | Validated as a local negative control | four-edge charge-two grammar, exact local downfolding and complete Pauli reward reject a clean disconnected pair seed: `XXXX`/`ZZZZ` coefficients are ~`1e-15` |
| Phase 7C optimizer monitoring and initial optimization state | Validated as an infrastructure control | JSONL/CSV/Matplotlib monitor records loss, gap, capture, Pauli selectivity and parameters; `optimizer_s0_xxxx.json` stores the complete registered initial observation |
| Phase 7C the submitted candidate mixed-species ring | Rejected as submitted and as a target primitive | raw payload falsely labels mixed mediator parities `[0,0]`; corrected-label counterfactual is connected but has `XXXX`/`ZZZZ` only ~`-1.25e-10` with dominant two-body terms ~`2.5e-3` |
| Phase 7C link-local charge-two channel grammar | Rejected as a selective local stabilizer primitive in the tested box | exhaustive fixed-scale catalog: 0/368 topologies has `|c_XXXX|>1e-7`; representative scaling gives `XXXX~g^5.99` against unwanted terms `~g^2.00` through `g/Delta=0.15` |
| Phase 7C crossed plaquette-support charge-two mediator | Local direction qualified; not promoted | exact block gives `XXXX~g^4`; fitted Z/ZZ counterterms suppress order-two diagonal content, but `c_XXXX=-2.03e-7` at g=0.5 and alignment is only 0.1135 because other four-body terms remain |
| Phase 7C crossed quartet monomial projection | Validated corrective audit; static branch closure strengthened | exact monomer projection identifies only the all-flip `F_p=|aaaa><bbbb|+h.c.` (2 of 16 `XXXX` monomials), with `F_p~g^3.998`; its squared `XXXX` alignment ceiling is `1/8`, independently reproducing archived `c_XXXX=-2.0304689728e-7` |
| Phase 7C crossed-mediator two-plaquette overlap | Rejected as a commuting stabilizer-parent geometry | the 64D monomer space remains isolated (`gap=39.94`, capture `0.9998`) and the dominant all-flip survives, but overlapping `F_p` have exact spectral commutator norm `1` |
| Phase 7C repeated all-flip algebra | Rejected as a commuting-parent route | exact logical-space control: distinct `F_p` terms have spectral commutator norm `1` for every nonzero support overlap; only disjoint or identical supports commute |
| Phase 7D-F Floquet pair-hopping route | Dynamic two-rung bridge qualified; phase not qualified | positive-detuning charge-two same/opposite-rail pulses compile signed `ZZ` and pair hopping with residual `3.19e-16`; a time-multiplexed extension, not frozen static ANTLER |
| Phase 7D-F multi-link Floquet compiler | Validated as a logical-sector compiler control | four-rung even/odd schedule preserves both parities and has measured Trotter scaling `~phi^1.99987`; no simultaneous-mediator crosstalk or many-body phase audit |
| Phase 7D-F four-rung explicit-mediator pulse/crosstalk control | Conditionally validated, with `t_leg=0` | 472-state fixed-charge evolution closes pulses at `7.34e-29`; at inactive coupling `0.01 g`, full-schedule leakage is `5.67e-8`; this is an idealized coherent crosstalk model |
| Phase 7D-F unrefocused leg hopping during pulses | Rejected for the tested bare schedule | opposite-rail Rabi closure is spoiled: at `t_leg=0.1`, leakage is `2.02e-4`; at `t_leg=1`, `1.98e-2` and bare logical parity residual `2.62e-2`; refocusing/recalibration is required |
| Phase 7D-F staggered phase echo against leg hopping | Validated as an ideal finite-block control | exact toggle identity preserves nearest-link pair conversion and flips leg hopping; with `t_leg=1`, `0.01g` crosstalk and 32 subcycles: leakage `5.74e-8`, parity residual `4.29e-6`; phase-kick hardware remains un-derived |
| Phase 7D-F finite onsite-potential phase kicks with pulse left active | Rejected in the registered bandwidth window | `kappa=100..800` does not approximate the ideal echo: leakage remains `1.66e-2..4.59e-2`; a fast/switchable or composite kick implementation is required |
| Phase 7D-F continuous pair-preserving CDT drive | Algebraically qualified; not yet a gate | link-weighted mediator potential makes pair commutator exactly zero and reduces worst-case leakage to `4.35e-4` at four cycles, but this remains far above the registered local-control target |
| Phase 7D-F CDT finite-frequency amplitude optimization | Closed negative in registered window | at four cycles, the first Bessel zero remains best for leakage (`4.44e-4`) across `xi=2.10..2.75`; no hidden amplitude sweet spot promotes a gate |
| Phase 7D-F direct Peierls-sign echo | Validated as a conditional finite-block control bridge | exact `phi_leg=0/pi` sign switching reproduces the ideal echo; at 32 subcycles with `t_leg=1` and `0.01g` crosstalk, leakage `5.74e-8`, parity residual `4.29e-6`; synchronous switching hardware is not derived |
| Phase 7D-F finite Peierls phase-ramp audit | Incomplete | the explicit smooth-ramp script is preserved, but execution exceeded the local limit before final metrics; no claim is promoted |
| Phase 7D-F CUDA finite Peierls-ramp completion | Validated as finite-block numerical ramp control; material control unqualified | 2/4/8 phase segments converge at observed leakage order `2.17`; at 8 segments, leakage is `8.82e-7` (0.5%) and `1.33e-5` (2%). Bandwidth, calibration, timing/phase noise and full-ladder integration are open |
| Phase 7D-F CUDA deterministic Peierls switching-error audit | Local tolerance bracketed; not a material-noise qualification | under pre-registered `1e-4` local targets, a common `pi`-plateau offset passes through `5 deg` and first fails at `7.5 deg`; signed time imbalance passes through `5%` and first fails at `10%` |
| Phase 7D-F CUDA quasi-static global control noise | Local statistical robustness curve completed; not material qualification | 50 seeded realizations per level: strict local target passes `50/50` at `1%`, `35/50` at `3%`, `20/50` at `5%`, `8/50` at `10%`, `2/50` at `20%` |
| Phase 7D-F full published Floquet-ladder benchmark | No candidate in the registered small-ED window | full high-frequency external model has parities below `2.33e-14`, but no `L=6` point passes both `split/gap < 1e-2` and bulk/edge transfer `<0.3`; not a refutation of the larger published analysis |
| Phase 7D-G 2D code-gadget route | Literature-motivated hypothesis; not yet specified microscopically | two-body toric-code gadgets require extra local encoding/ancilla and an independent derivation; the frozen ANTLER mediator grammar has not supplied it |
| Phase 8 canonical `U(1) x Z2` Floquet-ladder sectors | Validated as an external finite-MPS reproduction; not ANTLER-native | explicit parity tensors reproduce `L=8` ED per sector below `2.7e-14`; at `U0=-2, nu=1/4`, split falls `9.10e-3 -> 1.36e-3 -> 2.01e-4` for `L=8,12,16`, stable through `chi=384` |
| Phase 8 neutral-sector criterion | Reclassified as intentionally gapless for this external class | MPS neutral gap falls `0.302 -> 0.152 -> 0.090` through `L=16`, consistent with the reference's total-charge `c=1` mode; it cannot reject the relative topological sector |
| Phase 8 periodic charged-gap calibration | Encouraging small-ED external evidence; not thermodynamic | at published `U0=-1.5, alpha=0.5, nu=1/3`, periodic charge curvature is `0.381` (`L=6`) and `0.359` (`L=9`); infinite-MPS charged excitations remain required |
| Phase 8 published-point OBC parity scaling | Incomplete numerical convergence at longest size | fixed-density splits `0.212, 0.0758, 0.0357` for `L=6,12,18`, with ED check at `L=6`; `L=18` reaches `chi=384` and the `chi=512` audit timed out before serializing |
| Phase 8 finite-pulse stroboscopic Floquet bridge | Validated as a finite-block external protocol control | at ideal `eta=pi/2`, parity commutator `<5e-16` and `||U(T)-e^{-iTH_eff}||` scales as `T^1.976`; `0.1` rad angle error yields effective parity residual `0.112`; no ANTLER-native pulse claim |
| Phase 8 separate-mediator native `H0` bridge | Locally derived and exact at SW order; not yet an extended Hamiltonian | `theta=pi` ANTLER leg hopping matches fermionic `H0` at `2.45e-16`; two independent positive-detuned mediators per link give `U0=-g^2/Delta` with zero SW `aa->bb` term; exact error scales `~(g/Delta)^1.966` |
| Phase 8 native L=3 virtual-Rabi closure | Conditionally validated as an ideal microscopic control | shortest registered closure has leakage `2.16e-6`, polar distance `7.33e-5`, and strict timing window `[-0.3%,+0.3%]`; simultaneous-pulse hardware, longer ladders and noise remain open |
| Phase 8 native closure composition | Coherent-accumulation limit identified | at `n=1,2,4,8`, leakage grows `~n^1.996` and exceeds `1e-4` at `n=8`; symmetric Strang reduces coherent unitary distance but not leakage |
| Phase 8 direct switched-mediator `H1` segment | Finite-block implementation control; abrupt rail pulse not the dominant leakage source | direct coherent rotated pair channels factor `P^dag H0 P` to `3.51e-16`; at fixed `g/Delta=0.05` the `L=3` and `L=6` leakage remains comparable to the pulse realization and still accumulates |
| Phase 8 deep-SW fixed-logical-duration control | Validated finite `L=6,N=3` convergence; hardware feasibility unqualified | near-equal-duration Rabi-closed sequences give leakage `1.57e-5 -> 4.06e-6 -> 1.02e-6` for `g/Delta=0.05,0.025,0.0125`, fit exponent `1.970`; requires `Delta=600 -> 9600` and direct dynamic channels |
| Phase 8 direct-channel finite-ramp control | Conditionally validated as an ideal `L=3` bandwidth bracket | at deep SW and 16 cycles, linear and `sin^2` ramps through `1%` per transition pass strict local targets; both fail at `3%`; `sin^2` is less leaky and ramp discretization is converged |
| Phase 8 direct-channel angle calibration | Tight finite-block coherent-control bracket | registered leakage/distance/parity thresholds pass only through `+/-0.003` rad (`+/-0.172 deg`); `+/-0.01` rad breaks the parity criterion despite low leakage; no stochastic-material noise claim |
| Phase 8 direct-channel nearest-link crosstalk | Conditionally bounded, deterministic finite-block model | microscopic residual pair conversion passes through `0.3%` of `g` on `L=3,4` and fails the logical-distance threshold at `1%`; crosstalk chiefly induces coherent target error |
| Phase 8 correlated complex crosstalk ensemble | Statistical local bracket only | 100 seeded `L=4,N=2` samples per RMS level: `100/100` passes at `0.1%,0.2%`, then `89/100`, `72/100`, `39/100` at `0.3%,0.5%,1%`; one fully correlated coherent error model only |
| Phase 8 direct reused-mediator common microscopic `Z2` | Falsified for the registered direct implementation | exhaustive `L=3,N=2` assignments: `H0` only `x=[0,0,0,0]`, `H1` only `x=[0,1,0,1]`, intersection empty; best normalized residual `0.0705338`. This is not a no-go for fixed-charge separate mediator species. |
| Phase 8 direct segment-boundary parity law | Order-two boundary claim falsified for the registered Rabi-closed block | shared mediators give projected flip/polar-parity slope `5.931`, not `2`, over `g/Delta=0.20..0.0125`; a four-species fixed-charge control commutes microscopically below `1.7e-17` and has projected flips below `1.4e-17`. No node, ramp or thermodynamic extrapolation. |
| Phase 8 local Gauss-law algebra control | Validated as an abstract gate; no native implementation claimed | an inserted neutral ancilla realizes `G=(-1)^N_a X_g` with a two-dimensional physical sector and gap `3`: bare rail tunnelling projects to zero, but a local gauge-dressed tunnel remains non-scalar (norm `1`), so this is not a topological code. |
| Phase 7C the submitted candidate-discovered microscopic stabilizer primitive | Not established | no candidate has yet passed connected-target, operator-selectivity, independent SW and tiling gates |
| Effective Majorana T-junction braid | Architecture benchmark only | noncommuting braid target and finite-time dynamics; not derived from ANTLER |
| Phase-biased Kitaev T-junction | Static target benchmark only | four-zero-mode fixed-parity code, gap/noise/locality audits; pairing absent from frozen ladder |
| Non-Abelian braid algebra in an ANTLER-derived protected code | Not established | defect/fusion encoding and microscopic Hamiltonian bridge required |
| Phase 8 T-junction fusion/braid theoretical verdict | Theoretical verdict under revision; blocked by the absence of a common microscopic Z2 in the current direct realization | `docs/PHASE8_TJUNCTION_THEORY_VERDICT.md` rev.1: targeted no-go argument unchanged (class-(c) rail-parity operators, no superselection); conditional construction **not derived**  -  shared mediators admit no common charge assignment (exhaustive `L=3,N=2` audit: `H0` only `x=[0,0,0,0]`, `H1` only `x=[0,1,0,1]`, best compromise residual `7.05e-2`); Magnus term corrected to `-i(T/2)α(1-α)[H1,H0]`; ED/MPS contract T0–M1 remains registered |
| Phase 8 T-junction verdict, revision 2 correction | Supersedes the preceding revision-1 evidence text | the targeted algebraic no-go and the non-derived status remain. The shared-slot symmetry obstruction is exact; the universal boundary-law claim `~(g/Delta)^2` is withdrawn after the Rabi-closed audit measures `~(g/Delta)^5.931`. See `PHASE8_MICROSCOPIC_PARITY_AUDIT.md`. |
| Phase 8B Z2 Gauss minimal-resource theorem | Revised theory; only the repaired walker layer has passed independent ED | T1/T2 remain theoretical. The original single-mediator T3 is refuted. The revised Lambda-walker loop produces the abstract coarse `G_B` at order four with no non-scalar walker-layer parasite, but requires a newly declared density-conditioned neutral-walker primitive and has not passed Fock-block, resource-accounting or paired-transport gates. |
| Phase 8B stated single-mediator coarse-Gauss gadget | Refuted as written by exact selection-rule audit | for `H=Delta_G n_mu+lambda(mu†A+A mu)`, every low-to-low process has an even number of mediator conversions, so `H_eff=f(A²)` contains no `G_B=X_L P_B X_R`; `|c_G|<=2.78e-16`, while second-order pair products remain finite. T1/T2 and modified gadgets are not decided. |
| Phase 8B repaired Lambda-walker internal layer | Validated as an abstract internal-gadget control | a vacuum plus three-state neutral walker yields only `I` and `G_B=X_Lp_1p_2X_R` in the exact low branch; deep coefficient slope `3.973`, all other Walsh coefficients `<2.1e-17`. The density-conditioned walker hop is a new non-derived primitive. |
| Phase 8B Lambda-walker fixed-charge Fock block | Validated as a one-block inserted-primitive control | 96D `b=2,N=2` block: `[H,G_B]=0`, bare rail tunnel has zero physical-sector projection and unit sector-changing norm; Gauss-sector gap scales with deep power `3.973`. No inter-block pair transport or native primitive derivation. |
| Phase 8B two-block pair-only transport | Validated as a 3584D transport-algebra control | `[H,G_0]=[H,G_1]=0`; inserted pair transfer is physical-sector preserving while the single boundary hop is exactly sector-changing; minimum Gauss gap retains deep power `3.973`. No paired phase or native derivation. |
| Phase 8B two-block coherent pair dynamics | Validated as a single-pair dynamical control | at `g/Delta=0.025`, a pair transfers with maximum target population `0.99755` and Gauss-sector leakage `<3.5e-13`; virtual walker population falls with SW depth. No many-pair phase claim. |
| Phase 8B ideal pair-only many-body continuation | Rejected as a protected-phase route | the effective periodic hard-core-pair fluid has unique ground state and gaps closing with powers `-0.916` and `-1.043`; a strong-repulsion low doublet is locally readable (`||Pn_jP-scalar||=0.4683` at `B=12`). Projection and `V` are inserted, not native. |
| Phase 8B shared charge-two pair-link mediator | Validated as a local SW bridge | a positive-detuned mediator shared by two local pair channels yields `P_L^dag P_R+h.c.` at order two with coefficient-error power `1.982`, exact block parities, and no physical single-hop projection; its equal pair-projector shifts must remain in many-pair models. |
| Phase 8B derived pair-link with two Gauss walkers | Validated as a two-block one-pair control | a 3712D fixed-total-charge block replaces the formerly inserted pair transfer by the explicit mediator: at `g/Delta=0.025`, target population `0.99750`, Gauss leakage `<4.4e-11`, and both Gauss commutators vanish. New four-leg channel and Lambda walker resources are declared. |
| Phase 8B joint neutral star/plaquette walkers | Validated as an abstract local stabilizer gadget | exact 1024D six-link Schur audit gives `A_s` and `B_p` at powers `4.0365`, their commuting product at `8.0514`, and no Pauli word outside `{I,A_s,B_p,A_sB_p}` above `5.6e-17`. X/Z-conditioned walkers are new non-native primitives. |
| Phase 8B conditional effective toric tiling | Validated at effective stabilizer-spectrum level only | on the `3x3` torus, measured local coefficients keep the all-plus syndrome unique, GSD `4`, distance `3`, and a syndrome gap scaling `~(g/Delta)^4.037`; microscopic multi-gadget tiling/crosstalk and thermodynamic stability remain open. |
| Phase 8B coefficient-only walker braid family | Falsified as a non-Abelian route | every accessible effective Hamiltonian is a polynomial in the fixed commuting set `{A_s,B_p,A_sB_p}` and projects to `E_code I_4`; all 21 registered cross-point commutators vanish exactly. A support-changing e/m defect primitive is required. |
| Phase 8B mixed pentagon endpoint walker | Validated as an abstract local non-CSS mechanism | a 160D five-step phase-controlled walker isolates `XZXZX` with deep coefficient power `5.0136` and no other non-scalar local Pauli term; it is only a candidate endpoint building block. |
| Phase 8B pentagon/plaquette local branch-cut overlap | Validated as a one-overlap compatibility control | a 2560D joint audit of `XZXZXII` and `ZXIIIZX` has zero target commutator, powers `5.0502`, `4.0365`, `9.0613`, and no word outside its generated commuting algebra above numerical precision. No full twist geometry/fusion/braid. |
| Phase 8B programmable walker-loop lemma | Validated locally under its stated gadget assumptions | a length-`ell` neutral Pauli-conditioned loop first produces its full Pauli word at SW order `ell`; exact `ell=4,5` and overlap controls agree. This is a local compiler framework, not a full twist or native resource derivation. |
| Phase 8B coherent `Y` link | Validated as a local phase-control compiler primitive | an explicit 64D walker realizes `Y=iXZ` through a `pi/2` conditional phase and isolates `YXZX` at deep power `4.0091`; phase-stable neutral-walker hardware remains non-native. |
| Phase 8B central mixed-check/plaquette overlap | Validated as a second local twist-geometry compatibility control | a 1024D joint audit of `YXZXII` and `XZIIXZ` has zero target commutator, powers `4.0365`, `4.0365`, `8.0514`, and no word outside its generated algebra above numerical precision. No complete dislocation lattice, fusion space or braid. |
| Phase 8B finite mixed-check patch | Validated as an abstract finite-code completion | six four-link checks on seven links commute with rank 6, encode one qubit at distance 3, have exact syndrome gap `2J`, and reject all 210 Pauli probes below distance; only a finite algebraic control. |
| Phase 8B complete pairwise mixed-patch overlap gate | Validated within the ideal walker grammar | all 15 pairs are exact 2048D Schur audits at `g/Delta=0.05`; every block closes on its generated check algebra, with worst outside coefficient `1.66e-16`. No simultaneous six-walker ED or native derivation. |
| Phase 8B multi-walker closed-path selection rule | Validated as a formal perturbative grammar result | C4 vacuum-return paths have mod-two support only `I` or their full check; all ideal multi-walker SW terms lie in the 64-element stabilizer algebra, so they cannot create a new logical Pauli. This is not a finite-coupling convergence proof. |
| Phase 8B disjoint native walker bridge | Rejected as a conditional-walker realization | exact fixed-charge `2 x 3` block factorizes as rail-qubit plus mediator-walker, with static and complex-Floquet residuals below `8.7e-16`; phase programming alone cannot generate `W tensor X/Y/Z`. Shared-matter routes remain open. |
| Phase 8B shared-matter conditional walker link | Validated as a local downfolded compiler bridge | a fixed-charge six-mode block with shared rail/reservoir matter gives a sign-correlated SW echo `c_I I+c_XX X_q tensor X_walker`; `c_XX~(g/Delta)^2.0000`, isolated `IX`/`XI` cancel exactly in the registered model, capture reaches `0.99914` at `g/Delta=0.025`, and no other non-scalar Pauli coefficient is serialized. Dynamic finite-pulse closure, full four-state walker, patch integration and all topological/braid claims are open. |
| Phase 8B shared-matter pulse closure and phase axis | Rejected as a gate for the registered integer-Rabi schedule | at `g/Delta=0.025`, `BA` has low leakage but its physical polar signal is only `2.74e-7` against SW signal `3.93e-5` (`X/Y`) and `5.05e-7` against `7.86e-5` (`Z`); relative target errors are `1.0063`. The schedule returns populations but suppresses the intended conditional phase. |
| Phase 8B shared-matter `X/Y/Z` conditional-link library | Validated only as a static/downfolded one-link Pauli vocabulary | real/`pi/2`-phase rung hopping and a signed rail-potential bias respectively isolate `XX`, `YX`, `ZX` in the fixed-charge Schur block; every target has deep power `2.0000` and no serialized non-scalar companion. Integer-Rabi pulse controls are explicitly rejected; no common C4 walker, simultaneous-link audit, code or braid. |
| Phase 8B shared-matter arbitrary two-segment duration box | Rejected as a nontrivial local gate search | exact `t_A,t_B in [0,8]` grids at `g/Delta=0.10,0.05,0.025` find no `XX` candidate with leakage `<1e-4` and nonzero-angle trace fidelity `>0.999`; top rows collapse to the minimum allowed `0.05`-rad target, i.e. near identity. This does not refute longer composite, off-resonant dressed or adiabatic controls. |
| Phase 8B shared-matter abrupt fast `A B` Floquet window | Rejected as a conditional-gate implementation | exact repeated switching at `g/Delta=0.05,0.025`, total duration `20,100`, and segment duration `0.025..0.4` has no passing row: the best relative physical-to-SW signal error is `14.2956` against a `0.1` screen, and every row also exceeds the `1e-4` leakage screen. This rejects this abrupt window only, not a derived smooth/dressed/composite control. |
| Phase 8B shared-matter SW/Floquet averaging identity | Falsified for the registered sign-correlated `A/B` grammar | the arithmetic static `average(H_eff(A),H_eff(B))` has `XX~(g/Delta)^2`, but `H_eff(average(H(A),H(B)))` has exactly `c_XX=0` and retains `IZ`; `|IZ|/|XX_static|=49.9542` at every registered ratio. Exact rapid products converge to the microscopic average (`6.03e-6` full-space distance at `tau=2.5e-4`). |
| Phase 8B shared-matter smooth echo and classical-control baseline | No local gate in the registered boxes | all 30 smooth `sin^2` zero-to-zero echo rows fail (`min` relative error `0.999278` for a `0.1` target); three seeded two-duration differential-evolution baselines over holds `[0,400]` also fail (`min` relative error `1.000318`). These are reproducible control-box closures, not a no-go for a newly derived control algebra. |
| Phase 8B shared-matter balanced four-sign group | Static algebra clean; dynamic local gate rejected in the registered box | all four SW words retain `XX` and their static group has no serialized companion, while the SW of their zero microscopic control average is entirely scalar. All 30 smooth group rows fail (`min` relative error `0.999239`), so sign balancing removes the two-word `IZ` spectator but does not realize the conditional phase. |
| Phase 8B JW-string conditional-rung bridge | Rejected as a frozen-ANTLER/charge-two result; retained as an odd-string algebraic control | a generalized bridge has clean dynamic `XZ` only when the charge-two mediator is assigned artificial string weight `1`; frozen adjacent-rung weight `0` and physical-charge weight `2` both give `c_XZ=0`. The missing ingredient is an explicit neutral/odd `Z2` gauge link, not a pulse optimization. |
| Phase 8B direct shared-matter C3 walker composition | Rejected as a selective multi-link walker grammar | a 1488D explicit three-reservoir ring generates closed `XXX`, but the leading `XIX` companion is larger at every deep point (`3.93x`, `8.33x`, `22.2x` for `g/Delta=0.10,0.075,0.05`); deep powers `7.05` target versus `4.56` parasite. A detuning scan `0.5..10` finds no selective point. This does not refute one-link X/Y/Z or a different derived cancellation/encoding. |
| Phase 8B C3 parity-group echo | Validated only as a static effective-Hamiltonian symmetrization | four even-parity sign words cancel all serialized non-scalar companions after independent Schur downfolding and retain `XXX`; the physical integer-Rabi eight-segment realization is rejected because its relative error to the SW signal is approximately one at every registered ratio. |
| Phase 8C separated neutral-link `Z2` star audit | Validated as a reference-algebra control only | A new declared neutral-link resource, not a charge-two ANTLER mediator, passes the exact 128D/48D/6D audit: `max ||[H,G_v]||=0`, `||[H,N]||=0`, bare-hop physical projection `2.78e-17`, but local density remains non-scalar (`0.5`) in the physical sector. The three-arm graph has no plaquette, code, fusion or braid claim. `results/phase8c/z2_star_gauss_audit.json`. |
| Phase 8C neutral-link one-plaquette preflight | Validated as a reference gauge/flux control only | The 256D square (`96D` at `N=2`, physical dimension `12`) has exact Gauss/U(1), `max ||[B_p,G_v]||=0`, a static flux-sector gap `1.7491`, and dynamical flux under the declared electric fields. Local density remains non-scalar (`0.5`), so no deconfined phase, code, anyon, fusion or braid is claimed. `results/phase8c/z2_plaquette_preflight.json`. |
| Phase 8C pure-gauge `Z2` code patch | Validated as an imposed Abelian code reference only | The neutral-link `3x3` torus has 18 links, stabilizer rank `16`, four ground states, distance `3`, exact syndrome gap `4`, and all 1,431 Pauli probes of weight 1--2 scalar or null after code projection. It is not derived from ANTLER and contains no `e<->m` wall, twist, fusion or braid. `results/phase8c/z2_code_patch_preflight.json`. |
| Phase 8C `e<->m` duality / false-wall preflight | Global symmetry calibrated; paired local wall remains open | On the `3x3` neutral-link torus, global lattice duality plus Hadamard maps all 9 stars to plaquettes and a nontrivial `Z` string to an `X` string. A static `pi` cocycle and a bounded partial-Hadamard mixed-check control both fail the support-changing criterion. No separated local dislocation, fusion or braid is claimed. `results/phase8c/em_duality_wall_preflight.json`. |
| Phase 8C triangle twist-to-boundary reference | Validated finite non-CSS reference only | A declared seven-neutral-qubit `[[7,1,3]]` stabilizer patch has one mixed junction `YZXIXII`, exact gap `2J`, and X/Z-to-mixed string deformations modulo that check. It terminates at a boundary, contains no separated twist pair or fusion space, and is not derived from ANTLER. `results/phase8c/triangle_twist_reference.json`. |
| Phase 8C four-corner twist code-space calibration | Validated external boundary-twist calibration only | Odd-distance rotated planar CSS reference patches at `d=3,5` have a locally indistinguishable GSD-2 code space, gap `2J`, and minimum logical support `3,5`; every Pauli below distance is scalar or null. Boundary-fixed corners are not mobile interior defects; no physical fusion measurement, braid or ANTLER derivation is claimed. `results/phase8c/corner_twist_fusion_calibration.json`. |
| Phase 8C interior graph-twist preflight | Validated external static interior-twist reference only | A Sarkar-Yoder vertex-qubit graph code with two removed edges has four degree-three interior vertices, `k=3`, GSD `8`, gap `2J` and no logical Pauli below distance `3` (`L=4`) or `4` (`L=6`). A neighbouring static graph has the same code parameters but 12 cross-check anticommutations, so defect motion/holonomy remains un-derived. `results/phase8c/interior_twist_graph_preflight.json`. |
| Phase 8C direct interior-twist interpolation | Rejected as protection-preserving motion | Exact `3x3` reference interpolation keeps an eight-state band and a minimum gap `1.1632`, but its projected one-qubit signal reaches `0.2937` at mid-path. It is gapped yet locally readable, so it establishes no protected transport, holonomy or braid. `results/phase8c/interior_twist_linear_deformation_audit.json`. |
| Phase 8C measurement-resolved interior-twist deformation | Validated external stabilizer reference only | Four `+1` final-check measurements map the `3x3` initial graph-code stabilizer span to the final one. Each step has rank `6`, GSD `8`, commuting generators and all 27 one-qubit Paulis scalar or null. Outcome-dependent frames, logical transport, physical measurement and braid remain open. `results/phase8c/interior_twist_measurement_deformation.json`. |
| Phase 8C interior-twist logical transport | Validated external one-move reference only | The six logical Paulis stay canonical symplectic representatives through all four measurements, end in the declared final-code centralizer, and all 16 outcome patterns have a Pauli-frame word. A second adjacent move, an exchange and a nonzero commutator are still absent. `results/phase8c/interior_twist_logical_transport.json`. |
| Phase 8C first interior-twist cycle closure | Rejected in the registered target-check grammar | Three reference legs have protected measurement sequences, but the fourth closing leg has no accepted order among 13,699 nonrepeating target-check sequences. The loop is open; no holonomy, exchange, commutator or braid is claimed. `results/phase8c/interior_twist_cycle_closure_audit.json`. |
| Phase 8C auxiliary conditional closed holonomy | Validated external one-loop reference only | Declaring one local `YIIIIIIII` measurement closes the four-leg reference cycle, preserves the single-qubit local gate and yields a nonidentity logical symplectic map. It is one conditional holonomy, not a second loop, nonzero commutator or braid. `results/phase8c/auxiliary_closure_holonomy.json`. |
| Phase 8C two auxiliary conditional holonomies | Validated external noncommutativity calibration only | A second closed measurement loop, requiring declared local `IIIIIIIZI` and `IIIYIIIII` checks in addition to the T5f `YIIIIIIII` check, returns to the same `3x3` graph code and induces a GF(2) symplectic map that does not commute with the T5f map. The canonical span comparator was corrected and all T5c--T5f scripts were replayed. This is not an exchange, braid, fusion test, hardware protocol or ANTLER derivation. `results/phase8c/second_auxiliary_holonomy_commutator.json`. |
| Phase 8C local-measurement-only false-holonomy control | Validated bounded negative control | Exhaustive `+1`-branch sequences from all 27 single-vertex Paulis through depth 6 yield no nonempty loop returning to the initial `3x3` graph-code span while retaining rank and the one-qubit local gate. The high-weight graph checks are operationally essential in this short grammar; longer circuits and all physical-topology claims remain open. `results/phase8c/local_measurement_only_control.json`. |
| Phase 8C ancilla graph-check circuit compilation | Validated external circuit reference only | All 19 distinct T5f/T5g graph checks (weights 4 or 6) compile exactly to one readout ancilla, basis rotations and 4 or 6 data-to-ancilla CNOTs; the two projector branches agree below `1.45e-15`. The inserted CNOT/readout resource has no ANTLER, locality, noise, hook-error or fault-tolerance qualification. `results/phase8c/ancilla_check_circuit_audit.json`. |
| Phase 8C ancilla-Z hook-error schedule screen | Validated restricted circuit control | Across all `4!`/`6!` CNOT orders for 24 recorded high-weight stages, every stage has an ordering where every propagated single-ancilla-Z suffix is detected or scalar; no unavoidable logical hook remains. This excludes only that narrow fault channel, not CNOT/readout noise, decoding, fault tolerance or ANTLER derivation. `results/phase8c/ancilla_hook_error_audit.json`. |
| Frozen ANTLER direct neutral-link derivation | Closed scoped no-go | The direct JW-crossing bridge has exactly zero `X_rail Z_link` for frozen string weight `0` and physical charge-two weight `2` at all registered Mott depths; only an artificial odd weight `1` yields the clean term. A neutral odd `Z2` link is a missing resource, not an optimization parameter. `results/phase8c/native_neutral_link_decision.json`. |
| ANTLER-Z2 extension | Pre-registered new architecture only | A neutral two-level link and microscopic dressed-hop mechanism are now explicit mandatory resources. Gauss, SW selectivity, 2D protection and physical defects must pass in order before any non-Abelian language. `docs/ANTLER_Z2_EXTENSION_CONTRACT.md`. |
| External `Z2`/Majorana/twist proposal triage | Direction selected with explicit boundaries | Minimal neutral gauge links are a viable **new-resource** reference direction. Pure `D(Z2)` fusion and a classical `pi` branch-cut T are abelian, so their claimed two-dimensional fusion spaces are rejected. A genuine noncommutative target requires a qualified 2D `Z2` code plus an `e<->m` twist domain wall. Majorana and Higgs proposals remain external/new-resource architectures. See `docs/EXTERNAL_Z2_PROPOSALS_CRITICAL_TRIAGE_2026-07-20.md`. |
| Phase 8C recent literature decision | Active roadmap; no physics promotion | The 2025 number-conserving Floquet ladder is already the external Phase-8 benchmark and remains microscopically blocked in the registered ANTLER realization. Recent D4/Fibonacci/S3 hardware results separate noncommutative braiding from universality: the `D(S3)` 2026 demonstration uses braiding plus fusion and requires non-Abelian six-state links, a future new architecture. The near route remains `Z2` code then `e<->m` twists. See `docs/PHASE8C_RECENT_LITERATURE_DECISION_2026.md`. |
| Two-logical-qubit entangler | Not established | future work |
| Fault-tolerance threshold | Not established | future work |
